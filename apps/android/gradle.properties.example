# Copie os valores para ~/.gradle/gradle.properties, variáveis de ambiente
# ou passe por -P no Gradle.
# Nunca versione chaves reais.

DOOR_API_BASE_URL=https://ramoieeeufjf.dpdns.org
DOOR_API_KEY=preencher-localmente
